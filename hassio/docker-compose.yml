version: '3.2'

services:
  hassio:
    deploy:
      labels:
        - "traefik.enable=true"
        - "certificatesresolvers.default.acme.tlschallenge=true"
        - "traefik.http.routers.hassio.rule=Host(`fred-et-guillaume.cocooning.tech`)"
        - "traefik.http.routers.hassio.entrypoints=websecure"
        - "traefik.http.services.hassio.loadbalancer.server.port=8123"        
#        - "traefik.port=8123"
      replicas: 1
      placement:
        constraints:
          - node.role == manager
    image: homeassistant/raspberrypi3-homeassistant:0.114.3
#    volumes:
#      - type: volume
#        source: cocooning-nfs
#        target: /config
#        volume:
#          nocopy: false
    volumes:
      - type: bind
        source: /apps/hassio
        target: /config
    environment:
      - TZ=Europe/Paris
#    network_mode: host
    networks:
      - web
#    ports:
#      - "8123"
# créer le network cocooning-network avec le driver overlay (pas d'option)
networks:
  web:
    external: true
#volumes:
#  cocooning-nfs:
#    driver: local
#    driver_opts:
#      type: "nfs4"
#      o: "nfsvers=4,addr=192.168.1.100,rw,soft,nolock"
#      device: ":/apps/hassio"
